package com.neighbor.service.Transaction;

import com.neighbor.model.transaction.AcceptedOffer;

public interface AcceptedOfferService {
    AcceptedOffer createAcceptedOffer(AcceptedOffer acceptedOffer);
}
